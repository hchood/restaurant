<%= form_for @menu_item do |f| %>

  <% if @menu_item.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@menu_item.errors.count, "error") %> prohibited
      this menu item from being saved:</h2>
    <ul>
    <% @menu_item.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>

  <div>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div>
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div>
    <%= f.label :price_in_cents %>
    <%= f.text_field :price_in_cents %>
  </div>

  <div>
    <%= f.submit %>
  </div>


<% end %>
